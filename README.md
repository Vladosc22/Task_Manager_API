📌 Task Manager API

A simple RESTful Task Management API built with Spring Boot, JPA, and MySQL.

This project demonstrates:

Clean REST architecture

Entity relationships

JPA/Hibernate persistence

Global exception handling

Validation

Basic Spring Security integration

🚀 Tech Stack

Java 17

Spring Boot 3

Spring Web

Spring Data JPA

MySQL

Hibernate

Maven

Spring Security

📂 Project Structure
com.vlad.task_manager_api
 ├── task
 │    ├── Task.java
 │    ├── TaskRepository.java
 │    ├── TaskController.java
 │
 ├── user
 │    ├── User.java
 │    ├── UserRepository.java
 │
 ├── common
 │    ├── ApiExceptionHandler.java
 │
 └── TaskManagerApiApplication.java
⚙️ Setup & Run
1️⃣ Clone the repository
git clone https://github.com/YOUR_USERNAME/task-manager-api.git
cd task-manager-api
2️⃣ Configure MySQL

Create database:

CREATE DATABASE task_manager_db;
3️⃣ Set Environment Variables

Mac/Linux:

export DB_USERNAME=root
export DB_PASSWORD=your_password

Or run directly:

DB_USERNAME=root DB_PASSWORD=your_password ./mvnw spring-boot:run
4️⃣ Run the Application
./mvnw spring-boot:run

App will start on:

http://localhost:8080
🌐 API Endpoints
Get all tasks
GET /api/tasks
Create a task
POST /api/tasks

Example body:

{
  "title": "First task",
  "description": "Test task",
  "status": "TODO",
  "dueDate": "2026-03-01"
}
Update task
PUT /api/tasks/{id}
Delete task
DELETE /api/tasks/{id}
🛡 Error Handling

The project includes a global exception handler that returns structured JSON responses:

Example validation error:

{
  "timestamp": "2026-02-26T18:00:00Z",
  "status": 400,
  "error": "Bad Request",
  "validationErrors": {
    "title": "title is required"
  }
}
🔐 Security

Spring Security is included.

If no custom configuration is provided, a default generated password is printed in the console at startup.

🧠 What This Project Demonstrates

REST controller design

JPA entity relationships

Enum mapping with @Enumerated

Validation with jakarta.validation

Global exception handling with @RestControllerAdvice

Database integration with Hibernate

📌 Notes

This is a development/demo project.

No Flyway/Liquibase migrations included.

Schema is auto-generated by Hibernate.
